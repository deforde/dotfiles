require"lsp_signature".setup{
    bind = true,
    handler_opts = {
        border = "rounded"
    },
    hint_prefix = "",
    floating_window_above_cur_line = false,
    hint_enable = false,
    toggle_key = "<C-x>",
}

